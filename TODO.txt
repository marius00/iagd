Pending:
* Version number
* Assembly info like EvilSoft and IAGrim
* Remove any excess eqlite libraries
* Check what we need in terms of VC++ runtimes (can prolly remove some)
* Replace .net version for installer
* IAGD Icon
* Possibly patreon and gdrive icons (iagrim\properties\resources.resx)
* Ionic zip - Replace it, right now its hardcoded..
--- Message='IBM437' is not a supported encoding name. For information on defining a custom encoding, see the documentation for the Encoding.RegisterProvider method. (Parameter 'name')
* Spamming task cancelled:        private static AccessStatus IsTokenValid(string user, string token) {
* Sqlite does not support nested transactions:         public override void Save(IEnumerable<PlayerItem> items) {
